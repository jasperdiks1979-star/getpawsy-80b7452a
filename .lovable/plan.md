# Premium Viral Pinterest Pin Engine\n\nUpgrade the Pinterest pin generator from a single-template overlay system into a **6-style premium engine**, each pin type with its own distinct layout, copy bank, and visual DNA. Built on top of the existing production-verified pipeline (US scoring, warm-up cap, board routing, UTM, sanitize/QA layer, schema guards) — none of those guardrails change.\n\n---\n\n## Phase 1 — Supplier-image scrubbing (safety first)\n\nUpdate `supabase/functions/_shared/pinterest-qa.ts` (or add a new `pinterest-image-scrub.ts`) to detect and **reject** source images that look like supplier graphics:\n\n- Filename signals: `detail`, `size`, `chart`, `dimension`, `ruler`, `measure`, `_cm`, `_inch`, `package`, `sku`, `spec`, `aliexpress`, `cj-`, `taobao`.\n- URL host signals: `alicdn`, `aliexpress`, `cjdropshipping`, `taobao`, `1688`.\n- Aspect-ratio signal: very tall stitched composites (h/w > 2.0) — typical AliExpress measurement collages — auto-rejected.\n- Add a Cloudinary auto-crop fallback: when only such images are available, crop to the centered product square (`c_fill,g_auto,ar_4:5`) and skip if still flagged.\n\nReject pin generation for a product if zero clean images survive (returns `code: NO_CLEAN_IMAGE` in the batch report — no draft inserted).\n\n---\n\n## Phase 2 — 6 distinct visual templates\n\nReplace the single hook+CTA pill template with 6 unique Cloudinary composition functions in a new module `supabase/functions/_shared/pinterest-templates.ts`. Each returns a fully transformed image URL plus a layout signature (so we can dedupe layouts within the same product batch).\n\n| Style | Layout DNA |\n|---|---|\n| **problem** | Lifestyle backdrop, dark gradient overlay bottom-2/3, large emotional headline lower-left, no CTA pill (CTA in description) |\n| **before_after** | Vertical 50/50 split frame: left = supplier-image-only (cropped tight) labeled \