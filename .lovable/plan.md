## Pinterest Video Publisher — Build Plan\n\nA new pipeline that discovers existing MP4s in storage, classifies them, generates merchant-safe Pinterest metadata, and publishes them as real Pinterest **Video Pins** — fully isolated from the existing image-pin queue and TikTok systems.\n\n---\n\n### Scope guardrails (will NOT touch)\n- TikTok automation (`tiktok-*` functions, /admin/tiktok-*)\n- Existing image pin queue (`pinterest_pin_queue`, `pinterest-viral-batch`, `pinterest-creative-director`)\n- OAuth, board listing, Pexels, AI backdrop, dedup hashing for images\n- Cron worker for image pins\n\nEverything new lives behind a **separate `pinterest_video_*` namespace** and a separate edge function `pinterest-video-publisher`. The existing Pinterest OAuth tokens (`pinterest_accounts`) and board registry (`pinterest_boards`) are **read-only** consumers.\n\n---\n\n### Phase 1 — Discovery (`pinterest-video-discovery` edge function)\n- Lists all storage buckets (`pinterest-ads`, `pinterest-creative-director`, `tiktok