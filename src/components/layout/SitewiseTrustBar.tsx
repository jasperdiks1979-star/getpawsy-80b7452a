/**
 * SitewiseTrustBar — removed from global layout.
 * Trust signals are now shown inline on the homepage only (3 items).
 * This component intentionally renders nothing to avoid duplicate trust text.
 */
export function SitewiseTrustBar() {
  return null;
}
